name: CodeQL

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

permissions:
  contents: read

jobs:
  analyze:
    uses: github/codeql-action/.github/workflows/codeql.yml@v4
