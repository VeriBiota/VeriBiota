name: OpenSSF Scorecard

on:
  branch_protection_rule:
  schedule:
    - cron: "0 8 * * 1"
  push:
    branches: [main]

permissions: read-all

jobs:
  scorecard:
    uses: OmnisGenomics/.github/.github/workflows/scorecard.yml@main
