# Agentfile

## Commands
- build: `lake build`
- test: `lake build` (placeholder until dedicated tests exist)
- tasks: `npm run check:tasks`
- bench_sw: `./bench_sw`

## Notes
Follow task cards in `/tasks`. Update `.agent/state.json` when switching tasks.
